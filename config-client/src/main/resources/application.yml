spring:
  application:
    name: config-client
  profiles:
    active: local
  cloud:
    config:
      failFast: true
      name: config-client
      discovery:
        enabled: true
        serviceId: config-server